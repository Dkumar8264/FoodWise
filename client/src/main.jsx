import React, { useState } from 'react';
import { createRoot } from 'react-dom/client';
import { AnimatePresence, motion } from 'framer-motion';
import { Area, AreaChart, CartesianGrid, Cell, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Bell, CalendarDays, CheckCircle2, ChevronDown, ChevronRight, CircleHelp, ClipboardList, Eye, EyeOff, Leaf, LayoutDashboard, LockKeyhole, LogOut, Mail, Menu, Moon, MoreHorizontal, PackageOpen, Save, Send, Settings, Sparkles, Sun, TrendingDown, TrendingUp, UtensilsCrossed, X } from 'lucide-react';
import './styles.css';

const trend=[{day:'Aug 18',predicted:362,actual:348,wasted:25},{day:'Aug 21',predicted:390,actual:384,wasted:21},{day:'Aug 24',predicted:336,actual:329,wasted:19},{day:'Aug 27',predicted:405,actual:397,wasted:27},{day:'Aug 30',predicted:376,actual:365,wasted:18},{day:'Sep 02',predicted:421,actual:414,wasted:22},{day:'Sep 05',predicted:398,actual:401,wasted:15},{day:'Sep 08',predicted:430,actual:423,wasted:17},{day:'Sep 11',predicted:402,actual:394,wasted:14},{day:'Sep 14',predicted:456,actual:447,wasted:12}];
const dishes=[{dish:'Vegetable pulao',meal:'Lunch',predicted:180,override:180,unit:'plates',confidence:96},{dish:'Rajma chawal',meal:'Lunch',predicted:145,override:145,unit:'plates',confidence:94},{dish:'Masala dosa',meal:'Breakfast',predicted:120,override:120,unit:'plates',confidence:91},{dish:'Paneer curry',meal:'Dinner',predicted:92,override:92,unit:'kg',confidence:88},{dish:'Chapati',meal:'Dinner',predicted:340,override:340,unit:'pieces',confidence:93}];
const pie=[{name:'Rice',value:35,color:'#16a34a'},{name:'Breads',value:23,color:'#86efac'},{name:'Curries',value:19,color:'#fbbf24'},{name:'Snacks',value:13,color:'#fb923c'},{name:'Other',value:10,color:'#cbd5e1'}];
const nav=[['Dashboard',LayoutDashboard],['Predictions',Sparkles],['Analytics',TrendingUp],['Menu management',UtensilsCrossed],['Settings',Settings]];

function App(){
 const [active,setActive]=useState('Dashboard'),[open,setOpen]=useState(false),[dark,setDark]=useState(localStorage.getItem('foodwise-theme')==='dark'),[toast,setToast]=useState(''),[signedIn,setSignedIn]=useState(localStorage.getItem('foodwise-session')==='active');
 const notify=message=>{setToast(message);setTimeout(()=>setToast(''),2800)};
 const toggleTheme=()=>setDark(current=>{localStorage.setItem('foodwise-theme',current?'light':'dark');return !current});
 if(!signedIn)return <Login onSuccess={()=>{localStorage.setItem('foodwise-session','active');setSignedIn(true)}}/>;
 return <div className={'app '+(dark?'dark':'')}>
  <aside className={'sidebar '+(open?'show':'')}><div className="brand"><span className="brand-mark"><Leaf size={19}/></span><span>Food<span>Wise</span></span><button className="close" onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button></div><div className="workspace"><span className="tiny-label">WORKSPACE</span><button className="campus" onClick={()=>notify('North Campus Canteen is your active workspace.')}>North Campus Canteen <ChevronDown size={15}/></button></div><nav>{nav.map(([label,Icon])=><button key={label} onClick={()=>{setActive(label);setOpen(false)}} className={'nav-item '+(active===label?'selected':'')}><Icon size={19}/><span>{label}</span>{label==='Predictions'&&<span className="nav-dot"/>}</button>)}</nav><div className="side-bottom"><div className="impact"><div className="impact-icon"><Leaf size={18}/></div><div><b>146 kg saved</b><small>This month with FoodWise</small></div></div><button className="help" onClick={()=>notify('Support request opened — we will reply within one business day.')}><CircleHelp size={18}/> Help & support</button><button className="user user-button" onClick={()=>setActive('Settings')}><span className="avatar">AK</span><span><b>Arjun Kapoor</b><small>Canteen Admin</small></span><MoreHorizontal size={17}/></button></div></aside>
  {open&&<button className="scrim" onClick={()=>setOpen(false)} aria-label="Close navigation"/>}<main><header><button className="mobile-menu" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div className="crumb"><span>Overview</span><ChevronRight size={15}/><b>{active}</b></div><div className="header-actions"><button className="theme-toggle" onClick={toggleTheme} aria-label={dark?'Use light mode':'Use dark mode'}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button className="icon-button" onClick={()=>notify('No new notifications — your preparation plan is on track.')} aria-label="Notifications"><Bell size={19}/><i/></button><button className="profile" onClick={()=>setActive('Settings')} aria-label="Open profile settings">AK</button></div></header><AnimatePresence mode="wait"><motion.section key={active} initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-8}} transition={{duration:.22}} className="content">{active==='Dashboard'?<Dashboard go={setActive} notify={notify}/>:<Page page={active} notify={notify} dark={dark} toggleTheme={toggleTheme} logout={()=>{localStorage.removeItem('foodwise-session');setSignedIn(false)}}/>}</motion.section></AnimatePresence></main><AnimatePresence>{toast&&<motion.div className="toast" initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:12}}><CheckCircle2 size={19}/>{toast}</motion.div>}</AnimatePresence></div>
}
function Dashboard({go,notify}){const [rows,setRows]=useState(dishes),[filter,setFilter]=useState('All meals');const visible=filter==='All meals'?rows:rows.filter(row=>row.meal===filter);const edit=(dish,value)=>setRows(old=>old.map(row=>row.dish===dish?{...row,override:Number(value)||0}:row));return <><div className="page-intro"><div><p className="eyebrow">TUESDAY, SEPTEMBER 16</p><h1>Good morning, Arjun <span>— here’s your canteen pulse.</span></h1><p className="subcopy">Use tomorrow’s demand forecast to prepare smarter and waste less.</p></div><button onClick={()=>go('Predictions')} className="date-button"><CalendarDays size={18}/> Tomorrow, Sep 17 <ChevronDown size={15}/></button></div><div className="kpis"><Kpi title="Tomorrow’s demand" value="877" unit="plates & servings" icon={UtensilsCrossed} tone="green" delta="7.2%" note="vs. last Wednesday"/><Kpi title="Wastage this week" value="3.8" unit="% of food prepared" icon={TrendingDown} tone="orange" delta="1.4 pts" note="lower than last week"/><Kpi title="Estimated savings" value="₹12,640" unit="this month" icon={Leaf} tone="lime" delta="18.6%" note="vs. manual planning"/><Kpi title="Last prediction accuracy" value="94.2" unit="% accuracy" icon={Sparkles} tone="blue" delta="Excellent" note="Model confidence: high"/></div><div className="notice"><Sparkles size={18}/><div><b>Tomorrow looks busy.</b> An estimated 1,240 students are expected on campus due to the Cultural Fest. <button onClick={()=>go('Predictions')}>Review forecast</button></div></div><div className="grid-two"><Chart title="Demand performance" desc="Predicted vs. actual meals served" action={<button onClick={()=>notify('Showing the last 30 days of service data.')} className="select">Last 30 days <ChevronDown size={14}/></button>}><ResponsiveContainer width="100%" height={245}><LineChart data={trend}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip/><Legend verticalAlign="top" align="right" iconType="circle"/><Line type="monotone" dataKey="predicted" stroke="#16a34a" strokeWidth={3} dot={false}/><Line type="monotone" dataKey="actual" stroke="#334155" strokeWidth={2.5} dot={false}/><Line type="monotone" dataKey="wasted" stroke="#f59e0b" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></Chart><Chart title="Waste has trended down" desc="Daily food waste in kg" action={<button onClick={()=>go('Analytics')} className="quiet-button">View analytics <ChevronRight size={15}/></button>}><ResponsiveContainer width="100%" height={245}><AreaChart data={trend}><defs><linearGradient id="waste" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#86efac" stopOpacity=".7"/><stop offset="100%" stopColor="#86efac" stopOpacity=".04"/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="day" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip/><Area type="monotone" dataKey="wasted" stroke="#16a34a" fill="url(#waste)" strokeWidth={2.5}/></AreaChart></ResponsiveContainer></Chart></div><div className="forecast-card"><div className="forecast-head"><div><p className="eyebrow">AI FORECAST</p><h2>Tomorrow’s preparation plan</h2><p>Recommendations generated from recent consumption, schedule, and campus activity.</p></div><div className="forecast-actions"><select value={filter} onChange={e=>setFilter(e.target.value)} aria-label="Filter meal"><option>All meals</option><option>Breakfast</option><option>Lunch</option><option>Dinner</option></select><button onClick={()=>notify('Forecast saved and kitchen team notified')} className="primary-button">Save preparation plan</button></div></div><div className="table-wrap"><table><thead><tr><th>Dish</th><th>Meal</th><th>AI recommendation</th><th>Kitchen override</th><th>Confidence</th></tr></thead><tbody>{visible.map(row=><tr key={row.dish}><td><b>{row.dish}</b></td><td><span className="meal-tag">{row.meal}</span></td><td><b>{row.predicted} <small>{row.unit}</small></b></td><td><label className="override"><input value={row.override} type="number" onChange={e=>edit(row.dish,e.target.value)}/><span>{row.unit}</span></label></td><td><div className="confidence"><span><i style={{width:row.confidence+'%'}}/></span>{row.confidence}%</div></td></tr>)}</tbody></table></div><button className="link-button" onClick={()=>go('Predictions')}>View all 12 predictions <ChevronRight size={16}/></button></div><div className="bottom-grid"><Chart title="Where waste came from" desc="Last 30 days"><ResponsiveContainer width="100%" height={190}><PieChart><Pie data={pie} dataKey="value" nameKey="name" innerRadius={50} outerRadius={78} paddingAngle={3}>{pie.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="legend-list">{pie.slice(0,4).map(x=><span key={x.name}><i style={{background:x.color}}/>{x.name}<b>{x.value}%</b></span>)}</div></Chart><div className="action-card"><div className="action-icon"><ClipboardList size={21}/></div><p className="eyebrow">DAILY ROUTINE</p><h2>Log today’s service</h2><p>Record prepared, served, and leftover quantities before you close the kitchen.</p><button onClick={()=>go('Menu management')} className="outline-button">Log today’s data <ChevronRight size={16}/></button></div></div></>}
function Kpi({title,value,unit,icon:Icon,tone,delta,note}){return <article className="kpi"><div className={'kpi-icon '+tone}><Icon size={20}/></div><p>{title}</p><h2>{value}</h2><span className="unit">{unit}</span><div className="delta"><b className="green-text">{tone==='orange'?<TrendingDown size={14}/>:<TrendingUp size={14}/>} {delta}</b><small>{note}</small></div></article>}
function Chart({title,desc,action,children}){return <article className="chart-card"><div className="card-head"><div><h2>{title}</h2><p>{desc}</p></div>{action}</div>{children}</article>}
function Page({page,notify,dark,toggleTheme,logout}){const [saved,setSaved]=useState(false);if(page==='Settings')return <div className="workspace-page"><p className="eyebrow">WORKSPACE PREFERENCES</p><h1>Settings</h1><section className="settings-card"><div><b>Appearance</b><p>Choose your preferred FoodWise color mode.</p></div><button onClick={toggleTheme} className="date-button">{dark?<Sun size={17}/>:<Moon size={17}/>} {dark?'Light mode':'Dark mode'}</button></section><section className="settings-card"><div><b>Account</b><p>Signed in as admin@foodwise.in</p></div><button onClick={logout} className="danger-button"><LogOut size={16}/> Sign out</button></section></div>;if(page==='Menu management')return <div className="workspace-page"><p className="eyebrow">DAILY SERVICE RECORD</p><h1>Log today’s service</h1><form className="form-card" onSubmit={e=>{e.preventDefault();setSaved(true);notify('Today’s service record was saved.')}}><div className="form-grid"><Field label="Dish" value="Vegetable pulao"/><Field label="Meal" select values={['Lunch','Breakfast','Dinner']}/><Field label="Prepared quantity" value="190" type="number"/><Field label="Consumed quantity" value="176" type="number"/><Field label="Waste / leftovers" value="14" type="number"/><Field label="Campus event" value="Cultural Fest"/></div><button className="primary-button" type="submit"><Save size={16}/> Save service record</button>{saved&&<p className="success-copy"><CheckCircle2 size={16}/> Wastage rate for this entry: 7.4%</p>}</form></div>;if(page==='Predictions')return <div className="workspace-page"><p className="eyebrow">AI PREPARATION PLAN</p><h1>Tomorrow’s predictions</h1><p className="subcopy">Review recommendations and send the final plan to the kitchen team.</p><div className="forecast-card predictions-view"><div className="table-wrap"><table><thead><tr><th>Dish</th><th>Meal</th><th>Recommended quantity</th><th>Confidence</th></tr></thead><tbody>{dishes.map(row=><tr key={row.dish}><td><b>{row.dish}</b></td><td><span className="meal-tag">{row.meal}</span></td><td><b>{row.predicted} <small>{row.unit}</small></b></td><td>{row.confidence}%</td></tr>)}</tbody></table></div><button onClick={()=>notify('Preparation plan sent to the kitchen team.')} className="primary-button send-plan"><Send size={16}/> Send final plan</button></div></div>;return <div className="workspace-page"><p className="eyebrow">ANALYTICS</p><h1>Waste reduction analytics</h1><p className="subcopy">Your canteen is reducing wastage steadily. Export this report for the administration team.</p><div className="placeholder"><PackageOpen size={38}/><h2>Monthly report ready</h2><p>Waste is down 18.6% versus manual planning.</p><button onClick={()=>notify('Analytics report generated.')} className="primary-button">Generate report</button></div></div>}
function Field({label,value,type='text',select,values=[]}){return <label className="field"><span>{label}</span>{select?<select defaultValue={values[0]}>{values.map(value=><option key={value}>{value}</option>)}</select>:<input type={type} defaultValue={value}/>}</label>}
function Login({onSuccess}){const [email,setEmail]=useState('admin@foodwise.in'),[password,setPassword]=useState('foodwise123'),[show,setShow]=useState(false),[error,setError]=useState('');const submit=e=>{e.preventDefault();if(email==='admin@foodwise.in'&&password==='foodwise123')onSuccess();else setError('Use the demo credentials shown below to continue.')};return <main className="login-shell"><section className="login-panel"><div className="brand"><span className="brand-mark"><Leaf size={19}/></span><span>Food<span>Wise</span></span></div><div className="login-copy"><p className="eyebrow">CANTEEN OPERATIONS</p><h1>Plan smarter.<br/><em>Waste less.</em></h1><p>Sign in to make every meal count with data-led preparation planning.</p></div><div className="login-stat"><Leaf size={20}/><div><b>146 kg saved this month</b><span>across North Campus Canteen</span></div></div></section><section className="login-form-wrap"><form className="login-form" onSubmit={submit}><div><p className="eyebrow">WELCOME BACK</p><h2>Sign in to FoodWise</h2><p>Use your canteen admin account to continue.</p></div>{error&&<p className="form-error" role="alert">{error}</p>}<label className="auth-field"><span>Email address</span><div><Mail size={18}/><input autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} onBlur={()=>!email&&setError('Email is required.')} type="email" required/></div></label><label className="auth-field"><span>Password</span><div><LockKeyhole size={18}/><input autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} required/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><button className="primary-button login-submit" type="submit">Sign in <ChevronRight size={17}/></button><div className="demo-note"><b>Demo access</b><span>admin@foodwise.in · foodwise123</span></div></form></section></main>}
createRoot(document.getElementById('root')).render(<App/>);
